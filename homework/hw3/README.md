先檢查 (a\neq0)，再把方程式除以 (a) 使首項係數變成 1，接著透過代換 (x=t-\frac{b}{3}) 將原式化為沒有二次項的簡化形式 (t^3+pt+q=0)，並由此計算 (p)、(q) 與判別式 (\Delta)。之後套用 Cardano 公式，計算 (u^3=-\frac{q}{2}+\sqrt{\Delta}) 與 (v^3=-\frac{q}{2}-\sqrt{\Delta})，再用複數立方根與立方根單位根 (\omega) 組合出三個 (t) 的解，最後將結果回代 (x=t-\frac{b}{3})，得到原三次方程式的三個解（包含實根與複根）。

